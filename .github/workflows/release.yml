name: Release

# Cross-compile for 4 platforms and publish artifacts to a GitHub Release.
# Implemented in detail in a later stage — this is a working skeleton.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # Linux (x86_64, aarch64) and Windows are built in the same image as dev/CI.
  build-in-docker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            name: linux-amd64
            ext: ""
          - target: aarch64-unknown-linux-gnu
            name: linux-arm64
            ext: ""
          - target: x86_64-pc-windows-gnu
            name: windows-amd64
            ext: ".exe"
    steps:
      - uses: actions/checkout@v4

      - name: Build dev image
        run: docker compose build build-release

      # Run the same image as dev/CI, but without the target-cache volume, so
      # the binary is written to the host workspace where Package can find it.
      - name: Cross-compile ${{ matrix.target }}
        run: |
          docker run --rm -v "$PWD":/app -w /app \
            mikrotik-audit-mcp:dev \
            cargo build --release --locked --target ${{ matrix.target }}

      - name: Package
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          DIR=mikrotik-audit-mcp-${TAG}-${{ matrix.name }}
          mkdir -p "$DIR"
          cp target/${{ matrix.target }}/release/mikrotik-audit-mcp${{ matrix.ext }} "$DIR"/
          cp LICENSE README.md "$DIR"/
          tar czf "${DIR}.tar.gz" "$DIR"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: mikrotik-audit-mcp-*.tar.gz

  # Cross-building macOS from a Linux image is impractical (needs osxcross+SDK),
  # so we use a native macOS runner.
  build-macos:
    runs-on: macos-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            name: macos-amd64
          - target: aarch64-apple-darwin
            name: macos-arm64
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build ${{ matrix.target }}
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Package
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          DIR=mikrotik-audit-mcp-${TAG}-${{ matrix.name }}
          mkdir -p "$DIR"
          cp target/${{ matrix.target }}/release/mikrotik-audit-mcp "$DIR"/
          cp LICENSE README.md "$DIR"/
          tar czf "${DIR}.tar.gz" "$DIR"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: mikrotik-audit-mcp-*.tar.gz

  release:
    needs: [ build-in-docker, build-macos ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.tar.gz
          generate_release_notes: true
